plugins {
    id 'com.android.application'
}

android {
    namespace 'com.alanoffer.ahvaz'
    compileSdk 35

    defaultConfig {
        applicationId 'com.alanoffer.ahvaz'
        minSdk 24
        targetSdk 34
        versionCode 3
        versionName '0.3.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
        debug {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}
