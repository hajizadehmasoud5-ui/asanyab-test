You are editing the EXISTING DrLinq test project. Do not create a new project. Work only in this checkout and only change drlinq-test/implant-intake.html unless absolutely required.

GOAL: Fix page 2 (implant location) based on the user's latest approved direction.

REQUIRED CHANGES:
1) Jaw selection must be MULTI-SELECT, not radio/single-select.
   - Patient can select upper jaw only, lower jaw only, or BOTH upper and lower at the same time.
   - Each selected jaw must keep its own area selection independently: کامل / سمت راست / سمت چپ.
   - Deselecting one jaw must not erase or change the other jaw's selection.
   - Validation must allow both jaws and require an area for each selected jaw.
   - Summary must correctly show both jaws when both are selected.

2) Completely replace the current crude rounded-rectangle tooth chart on page 2.
   - Build a lightweight inline SVG/CSS dental chart that visually resembles actual human teeth, not pills or rectangles.
   - Use two clear dental arches/rows: upper and lower.
   - Use standard visible numbering like the user reference:
     upper: 18 17 16 15 14 13 12 11 | 21 22 23 24 25 26 27 28
     lower: 48 47 46 45 44 43 42 41 | 31 32 33 34 35 36 37 38
   - Each tooth must be independently clickable/tappable and show a very obvious green selected state.
   - Make tooth silhouettes differ enough by region (incisor/canine/premolar/molar) to feel human/dental rather than identical boxes.
   - Keep a simple red midline and small R/L orientation labels if helpful.
   - Do not use an external image for the actual selector. Use SVG/vector so every tooth is clickable.
   - Mobile-first: chart must fit/read well on phone; horizontal scrolling is acceptable only if necessary, but prefer a clean responsive fit.

3) Preserve everything else:
   - the existing 7-step intake flow
   - green DrLinq visual language
   - all real dental sample image assets/scripts, including lower-real.js
   - no backend changes
   - no production data changes
   - no new features beyond this page-2 fix

4) Stability is critical.
   - Do not leave the page blank.
   - Avoid fragile huge generated JS strings or malformed nested templates.
   - Keep the code simple and valid.
   - After editing, inspect the final HTML and ensure the page can render without JavaScript syntax errors.

Return only after the file is changed and locally checked.

RUN-ID: retry-3-reuse-existing-login