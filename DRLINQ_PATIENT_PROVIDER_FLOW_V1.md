# DrLinq — Patient & Provider Flow V1

هدف این نسخه: فرم بیمار ساده، قابل‌فهم و داینامیک بر اساس نوع درمان باشد؛ اولویت‌های بیمار رتبه‌بندی شوند؛ مدارک تصویری مرتبط با درمان با نمونه تصویری توضیح داده شوند؛ و خلاصه کیس به شکل استاندارد برای درمانگر ارسال شود.

## 1) تصمیم‌های قطعی طراحی

- اولویت‌ها دیگر multi-select ساده نیستند.
- بیمار ابتدا `اولویت اول` را انتخاب می‌کند و سپس `اولویت دوم` را به‌صورت اختیاری انتخاب می‌کند.
- اولویت دوم نمی‌تواند با اولویت اول یکسان باشد.
- مدارک تصویری بر اساس نوع درمان نمایش داده می‌شوند.
- کنار نام هر مدرک، یک thumbnail نمونه + توضیح خیلی کوتاه بیمارفهم نمایش داده می‌شود.
- برای هر مدرک سه پاسخ داریم: `دارم` / `ندارم` / `مطمئن نیستم`.
- اگر بیمار `دارم` را انتخاب کرد، دکمه آپلود همان مدرک نمایش داده می‌شود.
- فایل تصویری بیمار در مرحله اول برای همه درمانگرها ارسال نمی‌شود؛ ابتدا فقط وضعیت وجود مدرک نمایش داده می‌شود. مشاهده فایل بعد از اعلام تمایل درمانگر و طبق رضایت بیمار انجام می‌شود.

## 2) جریان عمومی بیمار

1. انتخاب نوع درمان
2. انتخاب شهر
3. اطلاعات اختصاصی همان درمان
4. اولویت اول
5. اولویت دوم (اختیاری)
6. مدارک مرتبط با همان درمان + نمونه تصویری
7. آپلود اختیاری مدارک موجود
8. زمان موردنظر برای مراجعه
9. شماره تماس
10. تأیید درخواست

## 3) اولویت‌ها

گزینه‌های نسخه اول:

- قیمت مناسب‌تر
- کیفیت / تجربه درمانگر
- زمان سریع‌تر
- امکان اقساط
- نزدیک‌تر بودن

UI پیشنهادی:

```text
مهم‌ترین اولویت شما چیست؟
[ قیمت مناسب‌تر ▼ ]

اولویت دوم شما چیست؟ (اختیاری)
[ زمان سریع‌تر ▼ ]
```

قانون اعتبارسنجی:
- `priority_1` الزامی
- `priority_2` اختیاری
- `priority_2 != priority_1`

## 4) مسیر ایمپلنت

### سوال‌های اختصاصی

- تعداد تقریبی ایمپلنت:
  - 1 واحد
  - 2 تا 3 واحد
  - 4 واحد یا بیشتر
  - نمی‌دانم

### مدارک

#### OPG
نمونه تصویری: `assets/examples/opg-example.webp`

متن بیمارفهم:
`عکس پانورامیک که همه دندان‌ها و فک را در یک تصویر نشان می‌دهد.`

پاسخ‌ها:
- دارم
- ندارم
- مطمئن نیستم

اگر `دارم`:
- نمایش دکمه `آپلود OPG`

#### CBCT
نمونه تصویری: `assets/examples/cbct-example.webp`

متن بیمارفهم:
`تصویر سه‌بعدی فک و دندان که ممکن است برای بررسی دقیق‌تر ایمپلنت درخواست شده باشد.`

پاسخ‌ها:
- دارم
- ندارم
- مطمئن نیستم

اگر `دارم`:
- نمایش دکمه `آپلود CBCT`

## 5) مسیر ارتودنسی

### اطلاعات اختصاصی

- سن بیمار
- مشکل اصلی به زبان خود بیمار (اختیاری در V1)

### مدارک

#### OPG
نمونه: `assets/examples/opg-example.webp`

#### Lateral Cephalometry
نمونه: `assets/examples/lateral-ceph-example.webp`

متن بیمارفهم:
`عکس نیم‌رخ جمجمه و فک که معمولاً برای بررسی ارتودنسی استفاده می‌شود.`

#### عکس‌های داخل دهان
نمونه: `assets/examples/intraoral-example.webp`

متن بیمارفهم:
`عکس‌هایی از دندان‌ها از روبه‌رو و طرفین.`

#### عکس چهره
نمونه: `assets/examples/face-ortho-example.webp`

متن بیمارفهم:
`عکس روبه‌رو و نیم‌رخ صورت که ممکن است برای بررسی فرم فک استفاده شود.`

برای هر مورد:
- دارم
- ندارم
- مطمئن نیستم (برای OPG و Lateral Ceph)
- در صورت داشتن، دکمه آپلود

## 6) مدل داده پیشنهادی درخواست

```json
{
  "service": "ایمپلنت",
  "city": "اهواز",
  "case_size": "4+",
  "priority_1": "قیمت مناسب‌تر",
  "priority_2": "زمان سریع‌تر",
  "wait": "تا 3 روز",
  "contact": "09xxxxxxxxx",
  "documents": {
    "opg": {
      "status": "has",
      "file_id": null
    },
    "cbct": {
      "status": "has",
      "file_id": null
    }
  }
}
```

مقادیر معتبر `status`:
- `has`
- `no`
- `unsure`

## 7) خلاصه‌ای که درمانگر می‌بیند

### ایمپلنت

```text
درخواست جدید دکترلینک

خدمت: ایمپلنت
شهر: اهواز
تعداد تقریبی: 4 واحد یا بیشتر
اولویت اول: قیمت مناسب‌تر
اولویت دوم: زمان سریع‌تر
زمان مراجعه: تا 3 روز

مدارک:
OPG: ✓ دارد
CBCT: ✓ دارد

[مایلم بررسی کنم]
[امکان پذیرش ندارم]
```

در این مرحله:
- نام بیمار نمایش داده نشود.
- شماره بیمار نمایش داده نشود.
- فایل اصلی گرافی به‌صورت عمومی برای همه مراکز ارسال نشود.

## 8) رفتار پس از اعلام تمایل درمانگر

اگر درمانگر `مایلم بررسی کنم` را بزند:

1. وضعیت provider/request به `interested` تغییر کند.
2. درمانگر بتواند اطلاعات اولیه زیر را وارد کند:
   - اولین زمان پذیرش
   - امکان اقساط: بله/خیر
   - پیام کوتاه برای بیمار
3. بیمار گزینه مرکز را در صفحه پیگیری ببیند.
4. فقط پس از انتخاب مرکز توسط بیمار، ارتباط مستقیم فعال شود.

## 9) تغییرات لازم در Frontend فعلی

- حذف رفتار فعلی `multi=true` برای priorities.
- افزودن دو فیلد مجزا: `priority_1` و `priority_2`.
- افزودن موتور سوال‌های داینامیک بر اساس `service`.
- افزودن Card مخصوص Document با:
  - thumbnail
  - title
  - توضیح کوتاه
  - سه گزینه status
  - upload button شرطی
- نمایش summary نهایی قبل از ثبت.
- نمایش document status در کارت پیگیری بیمار.

## 10) تغییرات لازم در Backend فعلی

- جایگزینی `priorities` با `priority_1` و `priority_2`.
- اضافه‌کردن `documents` به payload درخواست.
- اضافه‌کردن validation برای تکراری نبودن دو اولویت.
- اضافه‌کردن flow داینامیک بر اساس service.
- اصلاح `summary()` و `outreach()` برای نمایش اولویت‌های رتبه‌بندی‌شده و مدارک.
- در V1 آپلود فایل می‌تواند جدا از endpoint چت پیاده‌سازی شود؛ file metadata باید با request_id ذخیره شود.

## 11) نمونه‌های تصویری

چهار asset اولیه لازم است:

- `opg-example.webp`
- `cbct-example.webp`
- `lateral-ceph-example.webp`
- `intraoral-example.webp`
- `face-ortho-example.webp`

قواعد تصویر نمونه:
- آموزشی و عمومی باشد، نه پرونده واقعی قابل‌شناسایی بیمار.
- هیچ نام، تاریخ، شماره پرونده یا اطلاعات شناسایی روی تصویر نباشد.
- thumbnail سبک و مناسب موبایل باشد.
- با کلیک روی آن modal بزرگ‌تر باز شود.

## 12) ترتیب پیاده‌سازی پیشنهادی

### فاز A — همین حالا
- priority_1 / priority_2
- فرم داینامیک ایمپلنت
- OPG / CBCT با نمونه تصویری و status
- summary درمانگر

### فاز B
- فرم ارتودنسی
- Lateral Ceph / intraoral / face photos
- آپلود فایل واقعی

### فاز C
- نمایش امن فایل برای درمانگر علاقه‌مند
- رضایت بیمار برای اشتراک فایل
- audit log مشاهده فایل

## 13) معیار Done برای فاز A

فاز A فقط وقتی کامل است که یک بیمار بتواند:

1. ایمپلنت را انتخاب کند.
2. اهواز را انتخاب کند.
3. تعداد تقریبی را مشخص کند.
4. اولویت اول و دوم را به ترتیب مشخص کند.
5. OPG و CBCT را با دیدن نمونه تشخیص دهد و وضعیتشان را ثبت کند.
6. درخواست را ثبت کند.
7. درمانگر در Admin خلاصه کیس را با همان ترتیب اولویت‌ها و وضعیت OPG/CBCT ببیند.

این نسخه برای شروع تست واقعی ایمپلنت در اهواز کافی است.
