{
  "name": "drlinq-marketplace-v1",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node prepare-secure-pages.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
