# حافظه فعال پروژه AlanOffer

آخرین به‌روزرسانی: 2026-08-18

## تصمیم محصول
نام: **AlanOffer | الان‌آفر**

AlanOffer یک سایت تخفیف یا دایرکتوری ساده نیست. هسته محصول اتصال **تقاضای واقعی خریدار** به **ظرفیت، زمان یا موجودی قابل فروش کسب‌وکار** است.

اصل اقتصادی:
- فوریت ساختگی ممنوع.
- تخفیف باید دلیل واقعی داشته باشد.
- کاربر متقاضی رایگان می‌ماند.
- درآمد اصلی در آینده از سمت کسب‌وکار و پس از اثبات نتیجه است.

## تصمیم جغرافیایی
MVP دیگر «فقط اهواز» نیست.
معماری محصول برای **کل ایران** در نظر گرفته شده است. شروع تست عملی می‌تواند از اهواز باشد، اما مدل داده و گفتگو نباید Ahvaz-only باشد.

## تصمیم UX فعال
آخرین تصمیم: **Chat-first**.

صفحه اصلی فقط یک گفتگو است. کاربر لازم نیست فرم و فیلترهای زیاد پر کند.
دو نقش اصلی:
1. متقاضی / خریدار
2. صاحب کسب‌وکار / فروشنده

سیستم از متن آزاد کاربر نیاز را می‌فهمد و فقط اطلاعات ناقص را مرحله‌به‌مرحله می‌پرسد.
ورودی باید فارسی، عربی یا انگلیسی را بپذیرد.

فایل رابط اصلی: `index.html`
نسخه مستقل `chat.html` بعد از انتقال به index حذف شده تا دو UI موازی وجود نداشته باشد.

## وضعیت فعلی فنی
Front-end Chat-first آماده است و به این API متصل می‌شود:
`https://python-0jatcc.cldv.dev/alanoffer`

endpointهای مورد انتظار:
- `GET /api/chat/health`
- `POST /api/chat/start`
- `POST /api/chat/message`

**این endpointهای Chat هنوز روی بک‌اند Cloudiva فعال نشده‌اند.**
بنابراین در وضعیت فعلی Front-end ممکن است پیام «نیاز به راه‌اندازی پشت‌صحنه» نشان دهد.

## پاکسازی 2026-08-18
معماری‌های قدیمی از مسیر فعال حذف شدند، از جمله:
- فرم جداگانه کسب‌وکار
- پنل قدیمی admin سمت وب
- match-engine مبتنی بر localStorage
- فایل‌های قدیمی مناطق اهواز
- `asanyab_data_1..5`
- geography loader مربوط به فرم قدیمی
- backend setup page قدیمی

Android و backend فعلی برای جلوگیری از حذف وابستگی حیاتی نگه داشته شده‌اند تا مرحله بعد بررسی و یکپارچه شوند.

## محدودیت اصلی همین لحظه
مانع شماره ۱ دیگر UI نیست؛ **بک‌اند Chat** است.

مرحله بعدی:
1. اضافه کردن endpointهای Chat به بک‌اند واقعی Cloudiva.
2. ذخیره session و داده ساختاریافته خریدار/فروشنده.
3. تشخیص اطلاعات ناقص و سؤال بعدی.
4. تست health/start/message واقعی از GitHub Pages.
5. بعد از اتصال واقعی، تست چند سناریوی خریدار و فروشنده و ایرادیابی.

## حلقه اصلی که باید اثبات شود
**کاربر خواسته‌اش را می‌گوید → سیستم نیاز را ساختاری می‌کند → فروشنده ظرفیت/آفر را می‌گوید → Match → اعلان/ارتباط → نتیجه واقعی.**

تا وقتی این حلقه در تست واقعی کار نکرده، قابلیت جانبی جدید اضافه نشود.

## مخزن
GitHub repo:
`hajizadehmasoud5-ui/asanyab-test`

GitHub Pages:
`https://hajizadehmasoud5-ui.github.io/asanyab-test/`
