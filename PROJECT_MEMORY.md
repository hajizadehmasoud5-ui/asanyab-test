# حافظه فعال پروژه DrLinq

آخرین به‌روزرسانی: 2026-08-20

## تعریف محصول
**DrLinq | دکترلینک**

دکترلینک یک سایت تخفیف یا دایرکتوری درمانگر نیست. هسته محصول:

**مشتری مناسب برای درمانگر مناسب.**

بیمار نیاز و شرایطش را ثبت می‌کند. دکترلینک بر اساس همان تقاضای واقعی، چند درمانگر مناسب پیدا می‌کند و فقط برای همان‌ها درخواست می‌فرستد. هر درمانگری که تمایل به بررسی کیس داشته باشد وارد گزینه‌های واقعی بیمار می‌شود.

## اصل اقتصادی
- همه درخواست‌ها می‌توانند ثبت شوند؛ کیس کوچک حذف نمی‌شود.
- شرایط ویژه تضمین‌شده نیست.
- کیس‌های با حجم/ارزش بیشتر احتمال بیشتری برای شرایط ویژه دارند.
- قیمت‌ها عمومی و مزایده‌ای نمی‌شوند تا جنگ قیمت ایجاد نشود.
- کسب‌وکار خودش درباره قبول کیس و شرایط تصمیم می‌گیرد.
- هدف پایین آوردن عمومی قیمت بازار نیست؛ هدف Match کردن تقاضای واقعی با ظرفیت مناسب کسب‌وکار است.

## تصمیم کلیدی بانک درمانگران
**بانک کامل کشور را از قبل نمی‌سازیم.**

بانک Demand-first است:
1. درخواست واقعی می‌آید.
2. سیستم شهر + خدمت + اولویت بیمار را می‌فهمد.
3. بانک موجود بررسی می‌شود.
4. اگر گزینه کافی نیست، همان لحظه برای همان درخواست درمانگر جدید از وب/اینستاگرام/منابع عمومی پیدا می‌شود.
5. درمانگر پیدا شده به بانک اضافه می‌شود.
6. نتیجه تماس (قبول، رد، بدون پاسخ) ذخیره می‌شود و بانک با تجربه واقعی بهتر می‌شود.

## جغرافیا
نسخه اول فقط **مراکز استان‌های ایران** را می‌پذیرد. تست عملی می‌تواند از اهواز شروع شود.

## مسیر بیمار در نسخه فعلی
1. چه خدمت/درمانی می‌خواهد؟
2. کدام مرکز استان؟
3. حداکثر دو اولویت: قیمت بهتر / کیفیت / سرعت / اقساط / نزدیکی
4. حجم تقریبی کار
5. مدت انتظار
6. شماره موبایل یا واتساپ

پس از ثبت، یک `requestId` می‌گیرد و وضعیت درخواست قابل پیگیری است.

## مسیر عملیات دکترلینک
برای هر درخواست:
1. ابتدا 3 درمانگر مناسب انتخاب شوند.
2. اگر پاسخ کافی نبود، 2 گزینه بعدی.
3. در صورت نیاز تا حداکثر حدود 8 گزینه گسترش یابد.
4. پیام استاندارد کیس از پنل کپی و با واتساپ/اینستاگرام ارسال شود.
5. نتیجه هر درمانگر ثبت شود: contacted / interested / rejected / no_reply / selected.
6. درمانگر به بانک قابل استفاده برای درخواست‌های بعدی تبدیل می‌شود.

## ارتباط با درمانگر
در MVP ارسال اولین پیام **دستی/نیمه‌دستی** است. اتوماسیون Cold DM انبوه فعلاً ساخته نمی‌شود. بعداً فقط فرایندی که در تست واقعی جواب داده اتوماتیک می‌شود.

کانال‌های اصلی:
- Instagram برای کشف و تماس اولیه
- WhatsApp برای ادامه ارتباط و عملیات

## فایل‌های فعال
- `index.html` — رابط بیمار
- `admin.html` — پنل داخلی عملیات
- `backend/alanoffer_blueprint.py` — Flask + SQLite، نسخه 0.4.0

Prefix بک‌اند فعلاً برای جلوگیری از شکستن استقرار قبلی `/alanoffer` باقی مانده است.

## داده‌های مرکزی
- `chat_sessions`
- `chat_records` — درخواست‌های واقعی
- `providers` — بانک درمانگرانی که از تقاضای واقعی ساخته می‌شود
- `request_providers` — ارتباط هر درخواست با درمانگران و نتیجه تماس

## وضعیت فنی همین لحظه
کد GitHub برای MVP Demand-first تکمیل شده است:
- ثبت درخواست بیمار
- کد پیگیری و مشاهده وضعیت
- محدودیت مراکز استان
- ذخیره اولویت و حجم کیس
- پنل اپراتور
- ساخت بانک درمانگر بر اساس درخواست
- ثبت وضعیت تماس با هر درمانگر
- تولید متن آماده ارسال برای درمانگر

**مانع باقی‌مانده فقط deployment بک‌اند روی Cloudiva است.** فایل `backend/alanoffer_blueprint.py` باید روی سرویس فعال جایگزین شود، متغیر `DRLINQ_ADMIN_TOKEN` تنظیم و سرویس Restart شود.

## تست بعد از Deployment
فقط این حلقه تست شود:

**بیمار درخواست ثبت می‌کند → درخواست در admin دیده می‌شود → 3 درمانگر برای آن پیدا و اضافه می‌شوند → پیام ارسال می‌شود → پاسخ درمانگر ثبت می‌شود → وضعیت درخواست بیمار تغییر می‌کند.**

تا قبل از موفقیت این تست واقعی، AI تفسیر عکس، اپ مستقل، پرداخت، مزایده، امتیازدهی پیچیده و اتوماسیون انبوه اضافه نشود.

## مخزن
`hajizadehmasoud5-ui/asanyab-test`

GitHub Pages:
`https://hajizadehmasoud5-ui.github.io/asanyab-test/`

دامنه ثبت‌شده:
`drlinq.ir`
