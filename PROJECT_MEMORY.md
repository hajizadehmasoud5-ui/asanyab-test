# حافظه فعال پروژه AlanOffer

آخرین به‌روزرسانی: 2026-08-18

## تعریف محصول
**AlanOffer | الان‌آفر**

هسته محصول: اتصال **تقاضای واقعی خریدار** به **ظرفیت، زمان یا موجودی قابل فروش کسب‌وکار**.

اصول ثابت:
- AlanOffer فقط سایت تخفیف یا دایرکتوری نیست.
- فوریت ساختگی ممنوع.
- آفر باید دلیل اقتصادی واقعی داشته باشد.
- متقاضی رایگان می‌ماند.
- درآمد بعداً از سمت کسب‌وکار و فقط پس از اثبات نتیجه طراحی می‌شود.

## جغرافیا
معماری MVP برای **کل ایران** است. تست اولیه می‌تواند از اهواز شروع شود، اما مدل داده و گفتگو Ahvaz-only نیست.

## UX فعال
تصمیم نهایی فعلی: **Chat-first**.

صفحه اصلی: `index.html`

دو نقش:
1. متقاضی / خریدار
2. صاحب کسب‌وکار / فروشنده

کاربر آزادانه متن می‌نویسد و سیستم فقط اطلاعات ناقص را مرحله‌به‌مرحله می‌پرسد. فارسی، عربی و انگلیسی به‌عنوان ورودی پذیرفته می‌شوند.

## بک‌اند فعال در مخزن
فایل واحد بک‌اند:
`backend/alanoffer_blueprint.py`

فناوری: Flask Blueprint + SQLite

endpointها:
- `GET /alanoffer/api/chat/health`
- `POST /alanoffer/api/chat/start`
- `POST /alanoffer/api/chat/message`

نسخه فعلی: `0.2.0`

این MVP فعلاً **deterministic / structured** است (`ai=false`) تا حلقه اصلی را بدون خطای LLM تست کنیم.

خریدار فعلاً این اطلاعات را تکمیل می‌کند:
- نیاز
- شهر
- محله/منطقه
- مهلت انتظار
- شماره تماس

فروشنده:
- نام کسب‌وکار
- محصول/خدمت/ظرفیت
- شهر
- محله/منطقه
- قیمت عادی و آفر
- ظرفیت و مدت اعتبار
- شماره تماس

داده‌ها در SQLite مرکزی ذخیره می‌شوند:
- `chat_sessions`
- `chat_records`

شماره تماس اعتبارسنجی پایه دارد و sessionهای قدیمی پاک می‌شوند.

## پاکسازی انجام‌شده
معماری‌های موازی و قدیمی حذف شدند:
- UIهای قدیمی buyer/provider/admin
- `chat.html` تکراری؛ Chat-first مستقیماً `index.html` است
- `match-engine.js` مبتنی بر localStorage
- فایل‌های مناطق و `asanyab_data_1..5`
- geography loader فرم قدیمی
- backend setup قدیمی
- Node backend (`server.js`, `launcher.js`, `package.json`, `Dockerfile`)

Android حفظ شده است و WebView همچنان نسخه وب را دریافت می‌کند.

## وضعیت دقیق همین لحظه
**کد Front-end آماده است. کد بک‌اند Chat هم در GitHub آماده است، اما هنوز روی Cloudiva جایگزین و restart نشده است.**

بنابراین مانع فعلی فقط deployment است، نه طراحی یا کدنویسی endpointها.

## قدم بعدی — فقط همین
1. فایل `backend/alanoffer_blueprint.py` را جایگزین فایل فعلی `alanoffer_blueprint.py` در سرویس Cloudiva کن.
2. سرویس را Restart کن.
3. تست کن:
   - `/alanoffer/api/chat/health`
   - یک مسیر کامل خریدار
   - یک مسیر کامل فروشنده
4. فقط بعد از موفقیت این تست، Match بین buyer و seller ساخته شود.

## حلقه‌ای که باید اثبات شود
**متقاضی نیاز را ثبت می‌کند → فروشنده ظرفیت را ثبت می‌کند → داده هر دو مرکزی ذخیره می‌شود → Match → اعلان/ارتباط → نتیجه واقعی.**

تا قبل از اثبات این حلقه، قابلیت جانبی جدید اضافه نشود.

## مخزن
`hajizadehmasoud5-ui/asanyab-test`

GitHub Pages:
`https://hajizadehmasoud5-ui.github.io/asanyab-test/`
